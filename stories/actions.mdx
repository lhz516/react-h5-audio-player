import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import { action } from 'storybook/actions'
import { SAMPLE_MP3_URL } from './utils'
import AudioPlayer from '../src/index.tsx'
import * as ActionsStories from './actions.stories';

<Meta of={ActionsStories} />

# Actions

Different event listeners of the player

Go to `Canvas` tab to see `Actions` logger

<Canvas>
  <Story of={ActionsStories.Actions} />
</Canvas>
