import { Meta, Story, Canvas } from '@storybook/blocks';
import { SAMPLE_MP3_URL } from './utils'
import AudioPlayer from '../src/index.tsx'
import PlayList from './playlist.tsx'
import * as PlaylistStories from './playlist.stories';

<Meta of={PlaylistStories} />

# PlayList

Show skip controls (`Previous` and `Next` buttons), and play a list of music

#### PlayList

`onClickPrevious` and `onClickNext` are provided for callback.

Source code: https://github.com/lhz516/react-h5-audio-player/blob/master/stories/playlist.tsx
(MDX doesn't support nested component)

<Canvas>
  <Story of={PlaylistStories.Playlist} />
</Canvas>
