import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { SAMPLE_MP3_URL } from './utils'
import AudioPlayer from '../src/index.tsx'

<Meta title="Layouts" component={AudioPlayer} />

# Layouts

Different layouts of the player

#### Default

<Preview>
  <Story name="Default">
    <AudioPlayer src={SAMPLE_MP3_URL} />
  </Story>
</Preview>

#### Width

Default width is `100%`, you can always overwrite it to any value that greater than `300px`.

<Preview>
  <Story name="Width">
    <div>
      <p>300px</p>
      <AudioPlayer style={{ width: '300px' }} src={SAMPLE_MP3_URL} />
      <p>400px</p>
      <AudioPlayer style={{ width: '400px' }} src={SAMPLE_MP3_URL} />
      <p>500px</p>
      <AudioPlayer style={{ width: '500px' }} src={SAMPLE_MP3_URL} />
      <p>600px</p>
      <AudioPlayer style={{ width: '600px' }} src={SAMPLE_MP3_URL} />
      <p>100%</p>
      <AudioPlayer src={SAMPLE_MP3_URL} />
    </div>
  </Story>
</Preview>

#### Show Skip Controls

`onClickPrevious` and `onClickNext` are provided for callback.

<Preview>
  <Story name="Show Skip Controls">
    <AudioPlayer src={SAMPLE_MP3_URL} showSkipControls={true} />
  </Story>
</Preview>

#### Hide jump Controls and Show Skip Controls

<Preview>
  <Story name="Show Jump Controls and Show Skip Controls">
    <AudioPlayer src={SAMPLE_MP3_URL} showSkipControls={true} showJumpControls={false} />
  </Story>
</Preview>

#### Hide all Controls

<Preview>
  <Story name="Hide all Controls">
    <AudioPlayer src={SAMPLE_MP3_URL} showJumpControls={false} />
  </Story>
</Preview>

#### Hide Volume

Useful for most mobile browsers because they don't support volume control

<Preview>
  <Story name="Hide Volume">
    <AudioPlayer src={SAMPLE_MP3_URL} customVolumeControls={[]} />
  </Story>
</Preview>

#### Hide Loop Button

Useful for most mobile browsers because they don't support volume control

<Preview>
  <Story name="Hide Loop Button">
    <AudioPlayer src={SAMPLE_MP3_URL} customAdditionalControls={[]} />
  </Story>
</Preview>

#### Hide Download Progress

<Preview>
  <Story name="Hide Download Progress">
    <AudioPlayer src={SAMPLE_MP3_URL} showDownloadProgress={false} />
  </Story>
</Preview>

#### Custom default current time and duration

<Preview>
  <Story name="Custom default current time and duration">
    <AudioPlayer src={SAMPLE_MP3_URL} defaultCurrentTime="Loading" defaultDuration="Loading" />
  </Story>
</Preview>

#### Hide filled progress

<Preview>
  <Story name="Hide filled progress">
    <AudioPlayer src={SAMPLE_MP3_URL} showFilledProgress={false} />
  </Story>
</Preview>

#### Show header and footer

<Preview>
  <Story name="Show header and footer">
    <AudioPlayer src={SAMPLE_MP3_URL} header="Now playing: Let it go!" footer="This is a footer" />
  </Story>
</Preview>

#### Custom icons

Pass custom icons as needed to the props `customIcons`. Below is an example to use custom play icon for the audio player.

```ts
CustomIcons {
  play?: ReactNode
  pause?: ReactNode
  rewind?: ReactNode
  forward?: ReactNode
  previous?: ReactNode
  next?: ReactNode
  loop?: ReactNode
  loopOff?: ReactNode
  volume?: ReactNode
  volumeMute?: ReactNode
}
```

export const sampleIcon = (<svg role="img" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><title>asciinema icon</title><path d="M1.61 0V24L22.39 12L1.61 0M5.76 7.2L10.06 9.68L5.76 12.16V7.2M12.55 11.12L14.08 12L5.76 16.8V15.04L12.55 11.12Z"></path></svg>)

<Preview>
  <Story name="Custom icons">
    <AudioPlayer src={SAMPLE_MP3_URL} defaultCurrentTime="Loading" defaultDuration="Loading" customIcons={{ play: sampleIcon }} />
  </Story>
</Preview>
